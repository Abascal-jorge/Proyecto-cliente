//Se importa el type del proyecto
import {} from "../../types";

export default (state, action)=>{
    switch (action.type) {
        /*case :
            return{
                //Codigo 
            }*/
        default:
            return state;
    }
}